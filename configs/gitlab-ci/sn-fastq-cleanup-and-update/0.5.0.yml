.sn-fastq-cleanup-and-update-0.5.0:
  variables:
    IMAGE_NAME: sn-fastq-cleanup-and-update
    IMAGE_VERSION: 0.5.0
    DOCKERFILE_DIR: dockerfiles/${IMAGE_NAME}/${IMAGE_VERSION}
  # TODO: It would be good if we could use DOCKERFILE_DIR in rules:changes. It didn't work when I tested it in GitLab 15.11.6-ee
  rules:
    - changes:
        - configs/gitlab-ci/sn-fastq-cleanup-and-update/0.5.0.yml
        - dockerfiles/sn-fastq-cleanup-and-update/0.5.0/*
        - .gitlab-ci.yml

test-sn-fastq-cleanup-and-update-0.5.0:
  extends:
    - .test
    - .sn-fastq-cleanup-and-update-0.5.0
  needs: [ "build-sn-fastq-cleanup-and-update-0.5.0" ]

build-sn-fastq-cleanup-and-update-0.5.0:
  extends:
    - .build
    - .sn-fastq-cleanup-and-update-0.5.0
  variables:
    CONTAINER_IMAGE_NAME: ${IMAGE_NAME}
  


scan-sn-fastq-cleanup-and-update-0.5.0:
  extends:
    - .container_scanning
    - .sn-fastq-cleanup-and-update-0.5.0
  variables:
    CS_IMAGE: ${CI_REGISTRY_IMAGE}/${IMAGE_NAME}:${IMAGE_VERSION}
    CS_DOCKERFILE_PATH: ${DOCKERFILE_DIR}/Dockerfile
  needs: [ "test-sn-fastq-cleanup-and-update-0.5.0" ]

scan-sn-fastq-cleanup-and-update-latest:
  extends:
    - .container_scanning
    - .sn-fastq-cleanup-and-update-0.5.0
  variables:
    CS_IMAGE: ${CI_REGISTRY_IMAGE}/${IMAGE_NAME}:latest
    CS_DOCKERFILE_PATH: ${DOCKERFILE_DIR}/Dockerfile
  needs: [ "test-sn-fastq-cleanup-and-update-0.5.0" ]
