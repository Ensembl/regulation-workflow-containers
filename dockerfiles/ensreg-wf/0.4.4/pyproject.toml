[project]
name = "ensreg-wf"
version = "0.4.4"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "logfire>=4.22.0",
    "polars>=1.38.1",
    "pydantic>=2.12.5",
    "typer>=0.21.1",
]

[project.scripts]
ensreg-wf = "ensreg_wf.main:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
